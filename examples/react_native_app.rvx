// ⚛️ Ritam React Native App Example - Written in Tamil
// A React Native app demonstrating JSX compilation

// மாறிகள் (Variables)
மாறி தலைப்பு = "ரிதம் React Native"
மாறி எண்ணிக்கை = 0
மாறி பயனர் = "உலகம்"

// முதன்மை கூறு (Main App Component)
கூறு முதன்மை {
    காண்பி "div" {
        பண்பு "class" "container"
        
        காண்பி "h1" "🙏 வணக்கம் " + பயனர் + "!"
        காண்பி "p" "இது " + தலைப்பு + " பயன்பாடு."
        
        காண்பி "div" {
            பண்பு "class" "counter-section"
            காண்பி "h2" "எண்ணிக்கை: " {
                எண்ணிக்கை
            }
            
            காண்பி "button" "➕ அதிகரி" {
                சுடுக்கு அதிகரி
            }
            
            காண்பி "button" "➖ குறை" {
                சுடுக்கு குறை
            }
            
            காண்பி "button" "🔄 மீட்டமை" {
                சுடுக்கு மீட்டமை
            }
        }
    }
}

// அதிகரிக்கும் செயல் (Increment Function)
செயல் அதிகரி() {
    எண்ணிக்கை = எண்ணிக்கை + 1
}

// குறைக்கும் செயல் (Decrement Function)
செயல் குறை() {
    எனில் (எண்ணிக்கை > 0) {
        எண்ணிக்கை = எண்ணிக்கை - 1
    }
}

// மீட்டமைக்கும் செயல் (Reset Function)
செயல் மீட்டமை() {
    எண்ணிக்கை = 0
}

// சோதனை
#std.print("✅ React Native App compiled successfully!")
